# Supabase → Project Settings → Database → Connection string (URI).
DATABASE_URL=

# Resend (https://resend.com) — optional; omit all three to skip delete notification emails.
RESEND_API_KEY=
DELETE_TO_EMAIL=
RESEND_FROM_EMAIL=
