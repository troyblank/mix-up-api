input CreateListInput {
	name: String!
	type: ListType!
}

input InsertListItemInput {
	listId: ID!
	name: String!
}

type ListItem {
	id: ID!
	name: String!
}

enum ListType {
	pick
	list
}

type List {
	id: ID!
	name: String!
	type: ListType!
	items: [ListItem!]!
}

type Mutation {
	createNewList(input: CreateListInput!): List!
	insertListItem(input: InsertListItemInput!): ListItem!
}

type Query {
	lists: [List!]!
	list(id: ID!): List
}
